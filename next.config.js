/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove or comment out the static export
  // output: 'export',
  
  // If you have other configurations, keep them
}

module.exports = nextConfig 